FROM busybox:1.36

ARG CLANG_VERSION
ARG GCC_VERSION
ARG UBUNTU_VERSION
ARG UID
ARG GID

# Create a special file to make container images different
RUN echo "clang=${CLANG_VERSION} gcc=${GCC_VERSION} ubuntu=${UBUNTU_VERSION}" > /image_id

USER ${UID}:${GID}
WORKDIR /src
